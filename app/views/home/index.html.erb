<%= title("Home") %>

<div class="row-fluid">
	<div class="span12">
		<% if user_signed_in? %>
			Hi <%= current_user.email %>! <%= link_to "Logout", destroy_user_session_path, method: :delete %>
		<% end %>
	</div>
</div>

